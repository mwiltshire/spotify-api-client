const BASE = 'https://api.spotify.com';

export const PLAYER = `${BASE}/v1/me/player`;

export const QUEUE = `${PLAYER}/queue`;

export const DEVICES = `${PLAYER}/devices`;

export const RECENTLY_PLAYED = `${PLAYER}/recently-played`;

export const CURRENTLY_PLAYING = `${PLAYER}/currently-playing`;

export const PAUSE = `${PLAYER}/pause`;

export const SEEK = `${PLAYER}/seek`;

export const REPEAT = `${PLAYER}/repeat`;

export const VOLUME = `${PLAYER}/volume`;

export const NEXT = `${PLAYER}/next`;

export const PREVIOUS = `${PLAYER}/previous`;

export const PLAY = `${PLAYER}/play`;

export const SHUFFLE = `${PLAYER}/shuffle`;
